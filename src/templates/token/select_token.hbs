div style="text-align: center; margin: 1.5rem 0">
  {{localize "gurps.misc.applyTo"}}:
  <select>
    {{#each tokens}}
    <option value="{{this.name}}">{{this.name}}</option>
    {{/each}}
  </select>
</div>
