<div>
	<div class="placeholder"> </div>
	<select name="{{path}}.name.compare">
		{{#select this.name.compare}}
			{{#each config.select.string_compare as |v k|}}
				<option value="{{k}}">{{localize "gurps.field_prefix.name"}} {{localize v}}</option>
			{{/each}}
		{{/select}}
	</select>
	<div class="field">
		<input
			type="text"
			name="{{path}}.name.qualifier"
			value="{{this.name.qualifier}}"
			data-dtype="String"
			{{disabled (eq this.name.compare "none")}}
		/>
	</div>
</div>
<div>
	<div class="placeholder"> </div>
	<select name="{{path}}.notes.compare">
		{{#select this.notes.compare}}
			{{#each config.select.string_compare as |v k|}}
				<option value="{{k}}">{{localize "gurps.field_prefix.notes"}} {{localize v}}</option>
			{{/each}}
		{{/select}}
	</select>
	<div class="field">
		<input
			type="text"
			name="{{path}}.notes.qualifier"
			value="{{this.notes.qualifier}}"
			data-dtype="String"
			{{disabled (eq this.notes.compare "none")}}
		/>
	</div>
</div>
<div>
	<div class="placeholder"> </div>
	<select name="{{path}}.level.compare">
		{{#select this.level.compare}}
			{{#each config.select.number_compare as |v k|}}
				<option value="{{k}}">{{localize "gurps.field_prefix.level"}} {{localize v}}</option>
			{{/each}}
		{{/select}}
	</select>
	<div class="field">
		<input
			type="text"
			name="{{path}}.level.qualifier"
			value="{{this.level.qualifier}}"
			data-dtype="Number"
			{{disabled (eq this.level.compare "none")}}
		/>
	</div>
</div>
