<div>
	<div class="placeholder"> </div>
	<select name="{{path}}.name.compare">
		{{#select this.name.compare}}
		{{#each config.select.string_compare as |v k|}}
		<option value="{{k}}">{{localize "gurps.field_prefix.skill_name"}} {{localize (concat
			"gurps.select.string_compare." v)}}</option>
		{{/each}}
		{{/select}}
	</select>
	{{>sections/input-text field="field" name=(concat path ".name.qualifier") data=this.name.qualifier type="String"
	class=(input_lock (neq this.name.compare "none"))}}
</div>
<div>
	<div class="placeholder"> </div>
	<select name="{{path}}.specialization.compare">
		{{#select this.specialization.compare}}
		{{#each config.select.string_compare as |v k|}}
		<option value="{{k}}">{{localize "gurps.field_prefix.specialization"}} {{localize (concat
			"gurps.select.string_compare." v)}}</option>
		{{/each}}
		{{/select}}
	</select>
	{{>sections/input-text field="field" name=(concat path ".specialization.qualifier") data=this.specialization.qualifier
	type="String"
	class=(input_lock (neq this.specialization.compare "none"))}}
</div>
<div>
	<div class="placeholder"> </div>
	<select name="{{path}}.level.compare">
		{{#select this.level.compare}}
		{{#each config.select.number_compare as |v k|}}
		<option value="{{k}}">{{localize "gurps.field_prefix.level"}} {{localize (concat
			"gurps.select.number_compare." v)}}</option>
		{{/each}}
		{{/select}}
	</select>
	{{>sections/input-text field="field" name=(concat "NUMBER." path ".level.qualifier") data=this.levels.qualifier type="String"
	class=(input_lock (neq this.level.compare "none"))}}
</div>
