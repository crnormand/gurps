<div id='points'>
  <div class='header'>{{data.totalpoints.total}} {{i18n 'GURPS.points'}}</div>
  <div class='fieldblock'>
    {{#if isEditingA}}
      <div class='label'>{{i18n 'GURPS.pointsTotal' 'Total Points'}}</div>
      <div class='field noedit'><input
          name='data.totalpoints.total'
          class='gcs-input'
          type='number'
          value='{{data.totalpoints.total}}'
        /></div>
    {{/if}}
    <div class='label'>{{i18n 'GURPS.pointsUnspent' 'Unspent'}}</div>
    <div class='field noedit'><input
        name='data.totalpoints.unspent'
        class='gcs-input'
        type='number'
        value='{{data.totalpoints.unspent}}'
        {{disabled (not isEditing)}}
      /></div>
    <div class='label'>{{i18n 'GURPS.pointsRace'}}</div>
    <div class='field noedit'><input
        name='data.totalpoints.race'
        class='gcs-input'
        type='number'
        value='{{data.totalpoints.race}}'
        {{disabled (not isEditing)}}
      /></div>
    <div class='label'>{{i18n 'GURPS.pointsAttributes'}}</div>
    <div class='field noedit'><input
        name='data.totalpoints.attributes'
        class='gcs-input'
        type='number'
        value='{{data.totalpoints.attributes}}'
        {{disabled (not isEditing)}}
      /></div>
    <div class='label'>{{i18n 'GURPS.pointsAdvantages'}}</div>
    <div class='field noedit'><input
        name='data.totalpoints.ads'
        class='gcs-input'
        type='number'
        value='{{data.totalpoints.ads}}'
        {{disabled (not isEditing)}}
      /></div>
    <div class='label'>{{i18n 'GURPS.pointsDisadvantages'}}</div>
    <div class='field noedit'><input
        name='data.totalpoints.disads'
        class='gcs-input'
        type='number'
        value='{{data.totalpoints.disads}}'
        {{disabled (not isEditing)}}
      /></div>
    <div class='label'>{{i18n 'GURPS.pointsQuirks'}}</div>
    <div class='field noedit'><input
        name='data.totalpoints.quirks'
        class='gcs-input'
        type='number'
        value='{{data.totalpoints.quirks}}'
        {{disabled (not isEditing)}}
      /></div>
    <div class='label'>{{i18n 'GURPS.pointsSkills'}}</div>
    <div class='field noedit'><input
        name='data.totalpoints.skills'
        class='gcs-input'
        type='number'
        value='{{data.totalpoints.skills}}'
        {{disabled (not isEditing)}}
      /></div>
    <div class='label'>{{i18n 'GURPS.pointsSpells'}}</div>
    <div class='field noedit'><input
        name='data.totalpoints.spells'
        class='gcs-input'
        type='number'
        value='{{data.totalpoints.spells}}'
        {{disabled (not isEditing)}}
      /></div>
  </div>
</div>