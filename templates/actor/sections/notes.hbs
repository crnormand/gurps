<div id='notes' class='gga-table'>
  <div class='desc header' data-key='data.notes'>
    <div class='headermenu'>
      <span>{{i18n 'GURPS.notes'}}</span>
      <i class='fas fa-plus addnoteicon'></i>
    </div>
  </div>
  <div class='ref header'>{{i18n "GURPS.abbrevRef"}}</div>

  {{#each (flatlist data.notes)}}
    {{#if ../isEditing}}
      <div class='desc indent{{indent}} notemenu' data-key='data.notes.{{@key}}'>
        <textarea
          name='data.notes.{{@key}}.notes'
          class='gcs-input-ta'
          type='text'
          style='height:120px !important'
        >{{this.notes}}</textarea>
      </div>
      <div class='ref' data-key='data.notes.{{@key}}'>
        <input name='data.notes.{{@key}}.pageref' class='gcs-input-sm5' type='text' value='{{this.pageref}}' />
      </div>
    {{else}}
      {{#unless isCollapsed}}
        <div class='notedraggable notesmenu' data-key='data.notes.{{@key}}'>
          <div class='desc indent{{indent}} dblclkedit'>
            <div class='gga-desc-text'>
              <div>
                {{#if hasContains}}<i class='fas fa-caret-down expandcollapseicon'></i>{{/if}}
                {{#if hasCollapsed}}<i class='fas fa-caret-right expandcollapseicon'></i>{{/if}}
              </div>
              <div class='tooltip gga-manual'>
                {{#if this.notes}}
                  {{{gurpslinkbr this.notes}}}
                {{else}}
                  {{i18n "GURPS.noteBlank"}}<i class='far fa-laugh-wink'></i>
                {{/if}}
              </div>
              <div>
                {{#if (isUserCreated this)}}
                  <i class='fas fa-bookmark tooltip gga-usercreated'>
                    <span class='tooltiptext'>{{i18n "GURPS.equipmentUserCreated"}}</span>
                  </i>
                {{/if}}
              </div>
            </div>
          </div>
        </div>
        <div class='ref'>{{{pdflinkext this}}}</div>
      {{/unless}}
    {{/if}}
  {{/each}}
</div>