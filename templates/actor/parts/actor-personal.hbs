<section class="tab {{tab.cssClass}}" data-group="primary" data-tab="{{tab.id}}">
	<div id='personal-tab'>
		{{>identity}}
		{{>miscellaneous}}
		{{>description}}
		{{>points}}
		{{>lifting}}
		{{>notes}}
	</div>
</section>
